# Customer Monitor Top Navigation

## Preserved checkpoint

Customer Monitor, Alerts, and Tracks are preserved in checkpoint `d7042b9`.

## Monitor route family

The top Monitor navigation remains selected on:

```text
/monitor
/alerts
/tracks
```

Only the top-navigation active-state calculation was changed. The secondary
left rail continues evaluating its own Objects, Alerts, and Tracks selection.

## Selected styling

The active top-navigation item:

- fills the complete top-bar height;
- uses a solid selected blue background;
- has no inset bottom line or underline;
- has no bottom gap;
- extends the selected Monitor background slightly beneath the curved logo
  edge.

## Shared wordmark

The Solid Tracker wordmark replaces `RadioTower` with `MapPin`.